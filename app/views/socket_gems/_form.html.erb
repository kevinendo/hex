<%= form_for(@socket_gem) do |f| %>
  <% if @socket_gem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@socket_gem.errors.count, "error") %> prohibited this socket_gem from being saved:</h2>

      <ul>
      <% @socket_gem.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :color %><br />
	<%= f.select :color, {"Artifact" => "0", "Blood" => "1", "Wild" => "2", "Diamond" => "3", "Ruby" => "4", "Sapphire" => "5"}%>
  </div>
  <div class="field">
    <%= f.label :gem_type %><br />
	<%= f.select :gem_type, {"Minor" => "0", "Major" => "1"}%>
  </div>
  <div class="field">
    <%= f.label :game_text %><br />
    <%= f.text_area :game_text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
